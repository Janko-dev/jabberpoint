package Domain.Core.Content;

import Domain.Core.SlideShowComponent;

/**
 * Abstract base class for all slide items, i.e., {@code TextItem} and {@code ImageItem}
 * Currently, does not define state or behavior, but could be useful in the future to distinguish abstract slide item from its concrete leafs.
 */
public abstract class SlideItem extends SlideShowComponent {
}
